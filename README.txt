In order to start this application locally, move all files to the root
directory on the Apache Server and navigate to the index.php file
on the browser

For example:
localhost/<file-destination>/index.php
